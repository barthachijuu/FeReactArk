import { combineForms } from 'react-redux-form';

// eslint-disable-next-line import/prefer-default-export
export const defaultForm = combineForms({
  input: '',
}, 'defaultForm');
// this is only an example
