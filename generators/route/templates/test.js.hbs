/**
 *
 * Jest Unit Test for
 * {{ properCase routeName }} Route
 *
 */
import React from 'react';
import { shallow } from 'enzyme';
import {{ properCase routeName }} from '../components/{{ properCase routeName }}';

let baseProps;
let render;

beforeAll(() => {
  baseProps = {
    router: {
    },
  };
});

beforeEach(() => {
  render = shallow(<{{ properCase routeName }} {...baseProps} />);
});

describe('<{{ properCase routeName }} /> rendering', () => {
  it('should render self', () => {
    expect(render).toHaveLength(1);
  });
});
