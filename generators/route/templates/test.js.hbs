/**
 *
 * Unit Test for
 * {{ properCase routeName }}
 *
 */
import React from 'react';
import { {{#if hasHistory}}mount, {{else}}shallow{{/if}} } from 'enzyme';
import {{ properCase routeName }} from '../components/{{ properCase routeName }}';

{{#if hasHistory}}
  let wrapper;
{{else}}
  let render;
{{/if}}
beforeEach(() => {
    {{#if hasHistory}}
    jest.mock('../../../../../config/__mocks__/jestMock.js')
    wrapper = mount( <{{ properCase routeName }} /> , require('../../../../../config/__mocks__/jestMock.js').default)
    {{else}}
    render = shallow(<{{ properCase routeName }} />)
    {{/if}}
});

describe('<{{ properCase routeName }} /> rendering', () => {
  it('should render self', () => {
    {{#if hasHistory}}
    expect(wrapper).toHaveLength(1);
    {{else}}
    expect(render).toHaveLength(1);
    {{/if}}
  });
});
