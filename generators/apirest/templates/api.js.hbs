export async function {{camelCase methodName}}(params) {
  let data;

  try {
    data = await axios({
    method: '{{type}}',
    url: `{{url}}`,
    data: { ...params },
    headers: { ...defaultHeaders },
    });
    return { response: data.data };
  } catch (error) {
    console.error(error);
    return { error: error.response.data };
  }
}
$&
