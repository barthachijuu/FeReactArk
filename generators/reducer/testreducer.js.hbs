/**
 *
 * Jest Unit Test for
 * {{ camelCase actionNameNew }} Redux Modules
 *
 */

import {{ camelCase actionNameNew }}Reducer from '../{{ camelCase actionNameNew }}';

describe('reducers', () => {
  it('should create a sample test', () => {
    const text = 'Set state on reducer';
    expect(text).toEqual('Set state on reducer');
  });
});
