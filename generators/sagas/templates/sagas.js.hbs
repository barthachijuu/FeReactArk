/*
 * {{camelCase sagaNameNew}} Sagas
 *
 * @version 1.0.0
 * @author {{uname}}
 * @since {{since}}
 */

import { put, fork, take, call, all } from 'redux-saga/effects';
import { delay } from 'Store/sagas/index';

// @generator saga

// @generator saga:watch

/**
 * Root saga manages watcher lifecycle
 */
export default [
  // @generator saga:fork
];
