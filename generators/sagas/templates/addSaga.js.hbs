function* {{camelCase sagaName}}() {

}
$&
function* watch{{properCase sagaName}}() {
  while ({{#if actionWatch}}yield take(actionTypes.{{whichAction}}'){{else}}true{{/if}}) {
    yield call({{camelCase sagaName}});
  }
}
