/**
 *
 * Jest Unit Test for
 * {{ camelCase actionNameNew }} Redux Action
 *
 */

import * as actions from '../{{ camelCase actionNameNew }}';

describe('actions', () => {
  it('should create a sample test', () => {
    console.log(actions)
    const text = 'Action creator';
    expect(text).toEqual('Action creator');
  });
});

